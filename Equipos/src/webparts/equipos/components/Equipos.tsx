import * as React from 'react';
import { IEquiposProps } from './IEquiposProps';
import { escape } from '@microsoft/sp-lodash-subset';
import './App.css';

export default class Equipos extends React.Component<IEquiposProps, {}> {
  public render(): React.ReactElement<IEquiposProps> {
    return (
     <>
     </>
    );
  }
}
